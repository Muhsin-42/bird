const conf = {
  BASE_URL: String(process.env.NEXT_PUBLIC_BASE_URL),
  TENOR_API_KEY: String(process.env.NEXT_PUBLIC_TENOR_API_KEY),
  MONGODB_URL: String(process.env.MONGODB_URL),
};

export default conf;
